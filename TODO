Юра: где скрипт, лежащий в Arch?
Боря: надо перейти на другого внутреннего пользователя (не lav, а korinfer), удалить LOCUSER

remove *elf* from /etc/rpm/*/macros for Arch

На LinuxXP/2006/2007 код ошибки при install возвращается неверно
FC 4?

Добавить время сборки, чтобы понимать статистику
исправить: не использовать WINENUMVERSION (проблема - в Makefile для FreeBSD, Gentoo)
по умолчанию при ошибке всё равно размонтировать
сделать передачу основных и дополнительных пакетов снаружи (для установки)

сделать условной установку после сборки в других системах

+ последнюю цифру версии в имя файла - было бы очень уместно. - для freebsd/gentoo/slackware

Gentoo: Вносить в /etc/portage ?
Gentoo, FreeBSD: нет очереди, и возможности одновременной сборки
Надо блокировать при сборке

проверить лицензию: tahoma есть в Word Viewer 97, в Debian используется
тест блокировок написать под win
Тест скорости блокировок переписать под win
Проверить нормально ли возвращается ошибка при непересобираемости (смотреть по логу в RPM/log ? )
Не работает --links в rsync ?

Опубликовать систему сборки под разные платформы
Проверить работу меню
debhelper имеет ошибки

Доделать:
  - src класть тоже в Alpha
  - убрать раскладку по каталогам архитектуры при сборке
    (пока не тревожит)
  - русский язык в саморазархивирующихся архивах (перевод/ или убрать - указывать на файл который с собой?)
  - самораспаковка
  - Кривая ссылка из README ?
  - Slackware и FreeBSD остались с кривыми сборками (забыть)
  - Запретить исполнение на ftp и других разделах по возможности

=== run-архив ===
  - упаковывать вместе с HASP
  - все пакеты
  - инд. скрипт
  - запрос на установку


