Перейти на новый etersoft-build-utils
Запустить скрипт переименование пользователя
В удалённые системы так же надо входить под пользователем korinfer, а не builder
Для оперативности: Нужно вернуть сборку по заказу, если ещё не собрана или бинарник старее, чем src.rpm.
То же и с пакетами открытой части?
+ Нужно доделать проверку наличия файлов (основных, перечисленных)
Пересобрать закрытую часть
Обновить документацию в пакете и на ftp до 1.0.10
внеся основные изменения.


Список систем, которые являются псевдонимами, завести отдельно, чтобы
их игнорировать при сборке, но при этом автоматом создавать ссылки при сборке базовой для них системы -
чтобы создавать структуру каталогов с нуля.
(удаляем ссылки -> создаём заново по списку)


Для перепаковки src.rpm ставить во временный каталог (это больше к rpmbph относится, но и здесь к hasher тоже)
Юра: где скрипт, лежащий в Arch?
надо перейти на другого внутреннего пользователя (не lav, а korinfer), удалить LOCUSER

PACKAGEVERSION (версия), PROJECTVERSION (часть пути) вместо WINENUMVERSION

Нужно исправить и проверить работу robot
robot перенести потом в tools
сделать eterbuild/korinf

Наверное стоит отложить до 1.0.10, когда сборка будет не нужна
makeiso: адаптировать, исправить использование WINENUMVERSION

remove *elf* from /etc/rpm/*/macros for Arch

Gentoo, FreeBSD: нет очереди, и возможности одновременной сборки
Надо блокировать при сборке
